<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group>
        <item
            android:id="@+id/action_filter_by_favorite"
            android:title="Show only favorite"
            android:checkable="true"
            app:showAsAction="withText" />
    </group>
    <group>
        <item
            android:id="@+id/action_sort_rating"
            android:title="Sort by rating (Highest to Lowest)"
            app:showAsAction="never"></item>
        <item
            android:id="@+id/action_sort_popularity"
            android:title="Sort by popularity (Most to Least)"
            app:showAsAction="never"></item>
    </group>
</menu>